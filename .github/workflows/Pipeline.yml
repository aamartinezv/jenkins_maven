name : CI/CD Pipeline
on:
  push:
    branches: [feature/ci_setup]

jobs:
  build_and_push_jar:
    runs-on: ubuntu-latest
    permissions:
        contents: read
        packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: increment version
        run: |
          mvn build-helper:parse-version versions:set \
          -DnewVersion=\${parsedVersion.majorVersion}.\
          \${parsedVersion.minorVersion}.\${{ github.run_number }} \
          versions:commit
          
          STRING_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          
          echo "VERSION=$STRING_VERSION" >> $GITHUB_ENV
      
      - name: build image
        env:
          DOCKER_USER: benlevydockerinfinity
          DOCKER_PASSWORD: ${{ secrets.DOCKERHUB_PUSH }}
        run: | 
          docker build -t ${{ env.DOCKER_USER }}/hello_world_java:latest \
          -t ${{ env.DOCKER_USER }}/hello_world_java:${{ env.VERSION }}  .
          
          docker login -u ${{ env.DOCKER_USER }} -p ${{ env.DOCKER_PASSWORD }}
          docker push ${{ env.DOCKER_USER }}/hello_world_java:latest
          docker push ${{ env.DOCKER_USER }}/hello_world_java:${{ env.VERSION }}


      #- name: Set up Java and Maven environment
        #uses: actions/setup-java@v5.2.0
        #with:
          #java-version: 25
          #distribution: temurin
      
      #- name: Build and Publish jar file to packages
       # run: mvn deploy
        #env:
         # GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


